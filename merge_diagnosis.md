# Merge Diagnosis Snapshot

## Remotes

```
$ git remote -v
(no remotes configured)
```

## Branches

```
$ git branch -vv
* work 7d78ed3 feat: implement basic auth flows (sign-up / sign-in / refresh / user)
```

## Recent commits

```
$ git log --oneline -n 10
7d78ed3 (HEAD -> work) feat: implement basic auth flows (sign-up / sign-in / refresh / user)
e3572be Add project title to README
```

## Fetch attempt

```
$ git fetch origin
fatal: 'origin' does not appear to be a git repository
fatal: Could not read from remote repository.

Please make sure you have the correct access rights
and the repository exists.
```
